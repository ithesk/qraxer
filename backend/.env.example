# ===========================================
# QRaxer API - Configuración de Entorno
# ===========================================

# Servidor
PORT=3001
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=30m
JWT_REFRESH_EXPIRES_IN=7d

# Odoo Configuration
ODOO_URL=https://your-odoo-instance.com
ODOO_DB=your-database-name
ODOO_ADMIN_USER=api_service_user
ODOO_ADMIN_PASSWORD=api_service_password

# QR Security (HMAC)
QR_HMAC_SECRET=your-hmac-secret-for-qr-signing
QR_EXPIRATION_MINUTES=60

# Grupo autorizado en Odoo (res.groups xml_id)
ODOO_AUTHORIZED_GROUP=repair_scanner_user

# CORS - Dominios permitidos (separados por coma)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
